---
import { getCollection } from 'astro:content';
import '../../styles/global.css';

export async function getStaticPaths() {
  const features = await getCollection('features');
  return features.map((entry) => ({
    params: { category: entry.slug },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await entry.render();

// Category colors for styling
const categoryColors: Record<string, string> = {
  planMode: 'var(--category-planMode)',
  documentation: 'var(--category-documentation)',
  reasoning: 'var(--category-reasoning)',
  tests: 'var(--category-tests)',
  tools: 'var(--category-tools)',
};

const categoryColor = categoryColors[entry.data.categoryKey] || 'var(--category-planMode)';
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={entry.data.description} />
    <title>{entry.data.title} Features - Pick Your Agent</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&display=swap" rel="stylesheet">
  </head>
  <body>
    <nav class="navbar">
      <a href="/" class="logo">pickyouragent.dev</a>
      <div class="nav-links">
        <a href="/about">About</a>
        <a href="/docs">Docs</a>
      </div>
    </nav>

    <main class="feature-page">
      <a href="/" class="back-link">← Back to comparison</a>
      
      <article class="feature-content" style={`--category-accent: ${categoryColor}`}>
        <Content />
      </article>
    </main>
  </body>
</html>

